//3. Define a function to calculate x raised to the power y.

#include<iostream>
using namespace std;
void power(int x, int y);
int  main()

{
   power(11,3);
}

void power(int x, int y)
{ int res=1;
 for(int i=0; i<y;i++)
 {  res*=x;
}

cout<<"ans = "<<res;
}
